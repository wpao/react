

// 
const root = document.getElementById('root')

const element = (
    <div style={{
        width: 200, 
        height: 200, 
        backgroundColor: 'blue'
        // vendor prefix
    }}></div>
)
ReactDOM.render(element, root)